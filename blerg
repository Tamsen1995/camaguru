git add -A
git commit -m "[ADD] Adding the email activation part. I'm not sure it works because I haven't fully figured out the header functions yet. Also I still need to encrypt my password before sending it to the db"
git push origin email/activation