git add -A
git commit -m "[UPD] Signup form is coming together pretty well. I modularized some of the javascript code for the signup. now I need to add the insertion of the information into the database in PHP"
git push origin master